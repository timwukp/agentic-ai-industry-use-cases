.venv/
node_modules/
__pycache__/
*.pyc
.pytest_cache/
cdk.out/
infra/cdk/.lambda-staging/
web/dist/
web/.env.production
web/.env.local
web/*.tsbuildinfo
tests/e2e/screenshots/
tests/e2e/test-results/
# The chart-coverage census: the report is checked in as the evidence for the
# coverage claim, but its raw inputs are not — 26 full-page PNGs and one JSON per
# question are regenerated by the run, and the PNGs are ~1MB each.
tests/e2e/chart-audit/results/
tests/e2e/chart-audit/*.png
.DS_Store
