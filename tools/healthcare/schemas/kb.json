[
  {
    "name": "search_knowledge_base",
    "description": "Search the organization's clinical knowledge base (clinical protocols, medication safety protocols, HIPAA/PHI handling policy) and return the most relevant passages with source documents. Use for any question about clinical protocols, compliance rules, or data handling policy.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Natural-language search query"
        },
        "max_results": {
          "type": "integer",
          "description": "Maximum passages to return (max 20) (Default: 5)"
        }
      },
      "required": [
        "query"
      ]
    }
  }
]
