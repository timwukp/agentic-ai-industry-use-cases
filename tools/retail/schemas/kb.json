[
  {
    "name": "search_knowledge_base",
    "description": "Search the retailer's knowledge base (inventory management policy, supplier SLA standards) and return the most relevant passages with source documents. Use for any question about reorder policy, safety stock rules, ABC classification standards, or supplier performance requirements.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Natural-language search query"
        },
        "max_results": {
          "type": "integer",
          "description": "Maximum passages to return (max 20) (Default: 5)"
        }
      },
      "required": [
        "query"
      ]
    }
  }
]
